div(class='item' ng-repeat='post in posts')
  a(ng-click='openModal(post.image.file)')
    img(ng-src="{{post.image.thumbnail}}")
  p {{post.created}}
  a(ng-href='#/details/{{post.username}}', class='media-heading') {{post.username}}: 
  p {{post.description}}
  a(href="#/posts/{{post.id}}") Details
